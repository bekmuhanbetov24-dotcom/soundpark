services:
  postgres:
    image: postgres:16-alpine
    container_name: soundpark-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-soundpark}
      POSTGRES_USER: ${POSTGRES_USER:-soundpark}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-soundpark} -d ${POSTGRES_DB:-soundpark}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
